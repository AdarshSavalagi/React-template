# MDMS

```shell
npm install -D autoprefixer postcss postcss-nested postcss-preset-env
```

```shell
npm install -D prettier eslint-config-prettier eslint-plugin-prettier
```

## Setup so far

- routing
- i18n
- redux with persist
- axios
- postcss
- prettier & eslint
- indexdb
- react hook form with zod
